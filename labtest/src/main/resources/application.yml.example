spring:
  application:
    name: labtest-service
  rabbitmq:
    host: localhost
    port: 5672
    username: <username>
    password: <password>

server:
  port: 8083

management:
  endpoints:
    web:
      exposure:
        include: health, metrics
